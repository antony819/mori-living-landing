import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Mori Living | Japanese-Inspired Interior Design Hong Kong",
  description: "Transform your Hong Kong home with Japanese-inspired design. 100+ projects completed. Free consultation. HK$400K-1.2M projects.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
